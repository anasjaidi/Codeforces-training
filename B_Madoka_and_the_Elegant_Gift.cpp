#include <vector>
#include <iostream>

using namespace std;

void    solve()
{
    int n , m;
    cin >> n >> m;
    vector<string> s(n);
    for (size_t i = 0; i < n; i++) cin >> s[i];
    for (size_t i = 0; i < n; i++)
    {
        for (size_t j = 0; j < m; j++)
        {
            
        }
        
    }
    
}

int main()
{
    int n;
    cin >> n;
    for (size_t i = 0; i < n; i++)
        solve();
    
}